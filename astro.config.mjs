// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://tempsitee.netlify.app', // your Netlify site root
  base: '/index2/',                      // your subdirectory (very important)
});
